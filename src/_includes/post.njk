---
layout: "base.njk"
---

<h1> {{ title }} </h1>
<div class="postmeta"></div><p><i>Published on: <time datetime="{{ page.date | dateIso }}">{{ page.date | dateReadable }}</time></i></p>
   <p> {% for tag in tags %}
       <a href="/posts/tag/{{ tag | slugify }}">#{{ tag }}</a>
    {% endfor %}
	</p>
</div>
</br>
</br>
{{ content | safe }}
</br>
<hr></hr>

<p> If you would like to recieve these posts in your inbox, why not sign up to my <a href="https://mailchi.mp/5643ab85b338/dispatches-from-david-ralph-lewis">mailing list</a></p>
</br>
<hr></hr>
	{% if prevPost.url %}
		<p>
			<strong>Next</strong>:
			<a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a>
        | 
		<strong>Previous</strong>:
		<a href="{{ prevPost.url }}">{{ prevPost.data.title }}</a>
	</p>
	{% if nextPost.url %}
	{% endif %}
	{% endif %}